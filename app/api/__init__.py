"""
Пакет для api (Representation) слоя в Layer Architecture

Пакет содержит модули для:
- Сборки общего api (всех версий api)

Пакет содержит пакеты для:
- Сборки api v1 (всех роутеров v1)
"""
import logging

logger = logging.getLogger(__name__)
